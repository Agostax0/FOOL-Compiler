let
    class Test(){
        fun test:int()
        let
            fun inc:int(a:int)
            let
                var z:int = 1;
            in
                a+z;
            var z:int = 69;
        in
            inc(z);
    }

    var test:Test = new Test();
in
    print( test.test() ) ;
